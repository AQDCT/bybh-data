
{% set counter = 0 %}

{% for item in questions %}
   {% if counter == 0 %}
	{% set currentCategory = item.category.id %}
	<div class="btn-group"><a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
	{{item.category}}<span class="caret"></span></a>
	<ul class="dropdown-menu">
   {% endif %}

   {% if currentCategory != item.category.id %}
            </ul></div>
            <div class="btn-group"><a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
            {{ item.category }}<span class="caret"></span></a>
            <ul class="dropdown-menu">
            {% set currentCategory = item.category.id %}
    {% endif %}
    <li><a href='{{ path('singlequestion', {'id': item.id })}}{{selections}}'>{{ item.name }}</a></li>
{% set counter = counter + 1 %}
{% endfor %}

</ul></div>
<hr>
